[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "assistant-stt-worker"
version = "0.1.0"
dependencies = [
    "faster-whisper",
    "numpy"
]

[tool.setuptools]
py-modules = ["worker", "app"]
