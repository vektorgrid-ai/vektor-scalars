[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "assistant-routing-worker"
version = "0.1.0"
dependencies = [
    "ollama"
]

[tool.setuptools]
py-modules = ["worker", "app"]
